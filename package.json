{
  "private": true,
  "description": "Root package.json — used only for Husky pre-commit hooks. Not a Node.js project.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  }
}
