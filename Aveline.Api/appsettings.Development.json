{
  "Clerk": {
    "Authority": "https://inspired-warthog-8208.clerk.accounts.dev"
  },
  "AgentService": {
    "BaseUrl": "http://localhost:8000",
    "InternalToken": "change-me-internal-token"
  },
  "BusinessAnalytics": {
    "//": "Local development runs a single replica and IDistributedCache falls back to memory, so a shared cache is not required here. The window cap and cache TTL keep the shipped defaults.",
    "RequireSharedCache": false
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Vision": {
    "BaseUrl": "https://api.openai.com",
    "Model": "gpt-4o-mini"
  }
}
