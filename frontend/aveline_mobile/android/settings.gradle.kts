pluginManagement {
    val flutterSdkPath =
        run {
            val properties = java.util.Properties()
            file("local.properties").inputStream().use { properties.load(it) }
            val flutterSdkPath = properties.getProperty("flutter.sdk")
            require(flutterSdkPath != null) { "flutter.sdk not set in local.properties" }
            flutterSdkPath
        }

    includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("dev.flutter.flutter-plugin-loader") version "1.0.0"
    id("com.android.application") version "9.1.0" apply false
    id("org.jetbrains.kotlin.android") version "2.4.0" apply false
    // Generates the Firebase string resources (google_app_id, gcm_defaultSenderId,
    // ...) from android/app/google-services.json. Without it Firebase.initializeApp()
    // throws [core/no-app] and FirebaseMessaging.instance crashes the shell.
    id("com.google.gms.google-services") version "4.5.0" apply false
}

include(":app")
