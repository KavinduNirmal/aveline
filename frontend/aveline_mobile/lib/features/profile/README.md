# Feature: Profile

Dock tab showing the signed-in user's identity and account.

## Layers

### `presentation/screens/`
- `profile_screen.dart` — the Profile dock tab: avatar, name, email, role chips,
  and a sign-out action. Mirrors the web shell's user footer card.
